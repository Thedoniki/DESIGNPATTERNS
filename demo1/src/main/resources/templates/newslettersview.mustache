<!DOCTYPE html>
<html lang="en">
<head>
    <title>NEWSLETTERS</title>
    <meta charset="utf-8">

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</head>
<body>

<div class="container">
    <table class="table table-striped">

        {{#newsletters}}
            <tr>
                <td>ID</td>
                <td>{{id}}</td>
                <td>Topic</td>
                <td>{{topic}}</td>
                <td>Author</td>
                <td>{{author}}</td>
                <td>Text</td>
                <td {{text}}</td>
                <td><a href="/deletenewsletter/{{id}}">Delete</a></td>
                <td><a href="/editnewsletter/{{id}}">Edit</a></td>
                <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}"/>
            </tr>
        {{/newsletters}}
    </table>
</div>

<div class="container">
    <h2>CREATE A NEW NEWSLETTER</h2>

    <table class="table table-striped">

    </table>
</div>
<form method="POST" action="/addnewsletter">
    <table class="table">
        <tr>
            <td>Topic: <input type="text" name="topic"></td>
        </tr>
        <tr>
            <td>Author: <input type="text" name="author"></td>
        </tr>
        <tr>
            <td> Text: <input type="text" name="text"></td>
        </tr>
        <tr></tr>
        <td><input type="submit" name="submit" value="Create" class="btn btn-primary"></td>
        <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}"/>
    </table>
</form>

<div class="container">
    <h2>SUBSCRIBERS</h2>

    <table class="table table-striped">

        {{#Subscriber}}
            <tr>
                <td>{{id}}</td>
                <td>{{name}}</td>
                <td>{{email}}</td>
            </tr>
        {{/Subscriber}}
    </table>
    <td><a href="/">Show all subscribers</a></td>

</div>
</body>
</html>